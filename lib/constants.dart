/// Milliseconds between each tick
const int TICK_RATE_MS = 10;

/// How many widgets does a widget factory cost?
const int FACTORY_COST = 10;

/// Widgets per second each factory builds
const double FACTORY_BUILD_RATE = 0.1;

/// How many factories does one RAM cost?
const int RAM_FACTORY_COST = 1;

/// How much RAM does a single module provide?
const int RAM_CAPACITY = 1000;

/// Percentage
const double RAM_LOW_THRESHOLD = 0.9;
